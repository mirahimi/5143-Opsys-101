# GET query to API
# API selects directory name information for parameter from Database table
    # recursively go through the PID of each directory, append the directory name to the path?
# current directory is set to the newly selected directory name
# error is thrown if either parameter doesn't exist in directory table or is a file
